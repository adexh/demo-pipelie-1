pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the Code'
            }
        }

        stage('Test-1') {
            steps {
                echo 'Running the Tests'
            }
        }

        stage('Test-2') {
            steps {
                echo 'Running the Tests'
            }
        }
        
        stage('Test-2') {
            steps {
                echo 'Running the Tests'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the coode'
            }
        }
    }
}
